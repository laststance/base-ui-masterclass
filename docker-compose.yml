services:
  postgres:
    image: postgres:17-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: e2e
      POSTGRES_PASSWORD: e2e
      POSTGRES_DB: base_ui_e2e
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
